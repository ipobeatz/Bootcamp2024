name: field_analysis
description: "Field Analysis, yapay zeka kullanarak topraktaki veriyi inceleyip analiz eden ve en uygun tohumları belirleyen yenilikçi bir uygulamadır. İleri veri analiz tekniklerini kullanarak Field Analysis, doğru tohumları toprağın özel koşullarına uygun şekilde eşleştirerek en iyi mahsul verimini ve sağlığını sağlar."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  pie_chart: ^5.0.0
  flutter_svg: ^1.0.3
  cupertino_icons: ^1.0.6
  fl_chart: 0.36.0
  firebase_core: ^3.2.0
  firebase_auth: ^5.1.2
  tflite_flutter: ^0.10.4
  tflite: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icon.png
    - assets/ic.png
    - assets/iconx.jpeg
    - assets/corn.svg
    - assets/fertilizer.svg
    - assets/inventory.svg
    - assets/icon2.png
    - assets/simple_random_model.tflite